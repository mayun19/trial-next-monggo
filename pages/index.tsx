import type { NextPage } from "next";
import Layout from "../components/Layout";

const Home: NextPage = () => {
	return (
		<Layout pageTitle="Homepage">
			<h1 className="title">Mutiara</h1>
		</Layout>
	);
};
export default Home;
